name: Actions Runner Probe

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  slim:
    runs-on: ubuntu-22.04
    steps:
      - name: Runner probe
        run: |
          echo "runner=ubuntu-22.04"
          uname -a
          free -h || true
